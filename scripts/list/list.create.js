(function() {
    var app = angular.module('listModule');

    app.controller('ListCreateController', ['$scope', '$sce', '$interval', 'Listes', '$route',
        function($scope, $sce, $interval, Listes, $route) {
           

        }
    ]);
})()